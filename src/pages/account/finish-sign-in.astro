---
import FinishSignIn from '~/components/FinishSignIn.tsx'
import BaseLayout from '~/layouts/BaseLayout.astro'
import { SITE_CONFIG } from '~/lib/siteConfig'
import '~/lib/firebase'
---

<BaseLayout
  title={`Finishing sign-in — ${SITE_CONFIG.name}`}
  description="Completing email-link sign-in."
  noindex={true}
>
  <section class="prose-spine prose">
    <FinishSignIn client:load successPath="/account/" />
  </section>
</BaseLayout>
